# Intentionally empty: satisfies the chart's envFrom for a SQLite-only test.
# Apply only in your explicitly selected NEW test namespace.
apiVersion: v1
kind: Secret
metadata:
  name: ha-core-test-env
type: Opaque
data: {}
